# Ingestion dependencies only. Install the project instead of this file when
# you can -- `pip install -e .` also puts the `attention-panel` command on PATH
# and makes `python -m attention_panel.cli` work from any directory:
#
#     pip install -e .            # ingestion
#     pip install -e ".[model]"   # + modelling libraries
#     pip install -e ".[dev]"     # + pytest
#
# NOTE: the original requirements.txt in this repo was UTF-16 encoded (it came
# from a PowerShell `pip freeze`) and `pip install -r` could not read it. This
# one is plain UTF-8.

requests>=2.31          # Wikimedia AQS, MediaWiki Action API, Wikidata SPARQL
PyYAML>=6.0             # universe definitions live in YAML, not in code
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40        # primary free market data source; Stooq needs no client
